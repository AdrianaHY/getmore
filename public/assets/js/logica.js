var string = "";
var stringSeparado = string.split();
var stringReverso = stringSeparado.reverse();

function buscarLetraRepetida(stringSeparado, stringReverso){
  for(i=0; i < stringSeparado.length; i++){
    if(stringSeparado.length[i] == stringReverso[i]){
      console.log(false);
    }else(stringSeparado.length[i] != stringReverso[i]){
      console.log(true)
    }
    // for(j = 0; j < (stringSeparado.length-1); i--){
    //   if(stringSeparado[j] == stringSeparado[j+1]){
    //
    //   }
    }
  }

}
